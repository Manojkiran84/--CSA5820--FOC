#include <stdio.h>
int main()
{
    int n;
    int max = 0;
    printf("Enter a number (0 to 10): ");
    scanf("%d", &n);
    while (n != 0)
    {
        if (max < n)
        {
            max = n;
        }
        printf("Enter a number (0 to 10): ");
        scanf("%d", &n);
    }
    printf("Max is: %d", max);
}
